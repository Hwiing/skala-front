# Newsletter API endpoint used by a future backend integration
NEWSLETTER_API_URL=https://api.example.com/newsletter/subscribe

# Keep private API keys in .env only. Never expose them in browser JavaScript.
NEWSLETTER_API_KEY=replace-with-your-server-side-key

